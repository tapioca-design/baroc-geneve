{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Place list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nameurl</th>
                <th>Nameshort</th>
                <th>Namefr</th>
                <th>Nameen</th>
                <th>Address</th>
                <th>Telephone</th>
                <th>Email</th>
                <th>Maplatitude</th>
                <th>Maplongitude</th>
                <th>Googlemapref</th>
                <th>Website</th>
                <th>Imageplacemain</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('place_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nameUrl }}</td>
                <td>{{ entity.nameShort }}</td>
                <td>{{ entity.nameFr }}</td>
                <td>{{ entity.nameEn }}</td>
                <td>{{ entity.address }}</td>
                <td>{{ entity.telephone }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.mapLatitude }}</td>
                <td>{{ entity.mapLongitude }}</td>
                <td>{{ entity.googleMapRef }}</td>
                <td>{{ entity.website }}</td>
                <td>{{ entity.imagePlaceMain }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('place_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('place_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('place_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
