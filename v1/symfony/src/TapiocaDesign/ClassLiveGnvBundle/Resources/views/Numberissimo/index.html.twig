{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Numberissimo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Floatissimo</th>
                <th>Decimalissimo</th>
                <th>Decimalissimoprecision</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('numberissimo_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.floatissimo }}</td>
                <td>{{ entity.decimalissimo }}</td>
                <td>{{ entity.decimalissimoprecision }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('numberissimo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('numberissimo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('numberissimo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
